import * as React from 'react'

import * as style from './Title.scss'

class Title extends React.Component {
  render() {
    return <div className={style.title}>{`X`}</div>
  }
}

export default Title
